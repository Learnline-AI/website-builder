/**
 * EditorMode
 *
 * Entry point for the Visual Editor.
 * This wraps the EditorLayout component and provides page-level setup.
 */

import { EditorLayout } from './editor';

export function EditorMode() {
  return <EditorLayout />;
}

export default EditorMode;
